# Agent 模式浏览器复验记录（2026-08-14）

问法：

> 先查知识库里关于 LangGraph 和 AutoGen 的资料，再联网补充优缺点，最后整理成一份带对比表和引用的 Markdown 报告

环境：web `:3000` + agent `:3002`；已配置 `BOCHA_API_KEY`；Cursor Playwright。

## 验收清单（用户可见区）

| 项 | 结果 |
| --- | --- |
| 无任务中断 / 无 text-end 孤儿错误 | **通过** |
| 确定性流水线（无强制续跑） | **通过** |
| Markdown 对比报告 + 对比表 | **通过** |
| 正文无 `KB_SEARCH_STATUS` / `NO_RELEVANT_HIT` | **通过** |
| 知识库无依据说明 | **通过** |
| 参考来源含真实 `https://`（Bocha） | **通过**（正文可点链接 ≥1，本轮约 26 个 a[href^=http]） |
| 轨迹可下载；kb 无命中 tool 去重 | **通过**（NO_HIT 相关 1 条） |
| 轨迹可见联网结果摘要 | **通过**（如「联网 5 条 · …」） |

## 备注（非阻断）

- 部分参考链接质量一般（博客/知乎个人页），属搜索召回质量，非编排失败。
- 开放 Supervisor 路径仍保留强制续跑兜底（本问法走 Sequential，未触发）。

## 结论

**配置 Bocha 后的自动化验收通过。**
